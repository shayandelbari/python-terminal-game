# A Pacman like game with python
a game written in python using the Curses library for the terminal environment 

note: it only works in unix based system, apparently the Curses library is not in windows version of python at all
